Explanation of script tag and script.js file is given.

The benefit of a separate script file is that browser will download it and store it in its cache memory and also there can be separation of concerns.